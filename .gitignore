# Root design docs (not part of project)
/*.txt
AIOpsOS.md
__pycache__/
*.py[cod]
*.egg-info/
dist/
.venv/

# Node
node_modules/
# pnpm-lock.yaml should be committed for reproducible builds

# IDE
.idea/
.vscode/
*.swp

# Build / Cache
.vite/
.ruff_cache/

# Env
.env
*.env.local

# Claude
.claude/settings.local.json

# Docker / Runtime data volumes (legacy paths)
db_data/
redis_data/
kafka_data/
server_uploads/
server_data/
knowledge_data/

# Docker / Runtime data volumes (new unified path)
deploy/data/

# Server runtime data
server/data/knowledge/
server/data/logs/
server/data/cron_output/
server/data/skills/
server/.env

# Reflection pipeline staging area (R-3.14) — materialised skill candidates
# live under data/skills/.candidate/ until promotion. Explicitly ignored so
# the entry applies even when a developer un-ignores server/data/skills/ locally.
data/skills/.candidate/
server/data/skills/.candidate/

# Local test / tmp
tmp/

# Brainstorming artifacts
.superpowers/

# Test artifacts
server/text_*.json
