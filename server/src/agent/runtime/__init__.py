"""Agent runtime helpers (middleware, sub-agent factories).

Spec: .kiro/specs/agent-runtime-optimization-evolution — Phase J.

This package holds runtime-time plumbing that sits between the
DeepAgents / LangChain middleware abstractions and the rest of the
platform (prompt registry, trajectory sink, …).
"""
